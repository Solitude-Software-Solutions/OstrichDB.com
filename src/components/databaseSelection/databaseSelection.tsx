import React from 'react';

const DatabaseSelection: React.FC = () => {
    return (
        <div style={{ padding: '20px' }}>
            <h1 className='text-white'>Database Selection</h1>
            <p>Select a database to proceed:</p>
        </div>
    );
};

export default DatabaseSelection;